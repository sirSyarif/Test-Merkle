import { Icon } from "@iconify/react";

// sidebar config for list sidebar
const sidebarConfig = [
  {
    title: "User",
    path: "user",
    icon: <Icon icon="mdi:user-group" height={24} width={24} />,
  },
];

export default sidebarConfig;
